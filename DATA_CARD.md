# 멍탐정 데이터 카드

## 개요

멍탐정은 국가동물보호정보시스템의 구조동물 공고를 검색 가능한 메타데이터와 임베딩으로 변환합니다. 데이터는 입양 가능성을 보증하거나 개체의 성격·건강·사회성을 판정하기 위한 것이 아니라, 보호소 방문 전 공고 탐색을 돕기 위해 사용합니다.

## 원천 데이터

- 제공기관: 농림축산식품부 농림축산검역본부
- 서비스: 국가동물보호정보시스템 구조동물 조회 서비스
- 공식 문서: https://www.data.go.kr/data/15098931/openapi.do
- API base: `https://apis.data.go.kr/1543061/abandonmentPublicService_v2`
- 공공데이터포털 표시 이용허락범위: 제한 없음
- API 키: 저장소에 포함하지 않으며 `ANIMAL_API_KEY` 환경변수로만 전달

공공 API의 이용허락범위와 별개로 사진 자체에 제3자 권리가 있을 수 있으므로, 원격 사진을 별도 데이터셋으로 재배포하기 전에는 권리와 출처를 다시 확인해야 합니다.

현재 저장소의 기본 `full` 프로필은 공고 응답의 텍스트·메타데이터(`data/dog_metas.json`)와 사진·텍스트에서 만든 임베딩(`data/dog_faiss.index`)도 배포합니다. [공식 API 상세 페이지](https://www.data.go.kr/data/15098931/openapi.do)는 이 API의 이용허락범위를 `제한 없음`으로 표시하므로 텍스트·메타데이터는 그 표시와 출처를 근거로 사용합니다. [공공데이터포털 이용정책](https://www.data.go.kr/ugs/selectPortalPolicyView.do)은 제3자 권리가 포함된 데이터를 제공할 때 제공기관이 정당한 이용허락을 확보하고 범위를 표시하도록 안내합니다. 다만 API 응답에는 개별 사진의 촬영자·별도 라이선스가 없고 사진 파일은 별도 URL에서 내려받으므로, 사진·crop과 사진 파생 벡터까지 같은 범위로 공개 재배포할 수 있는지는 출품 전에 제공기관의 확인 응답을 보존하는 것이 안전합니다.

확인 응답을 얻지 못할 때 사용할 `public-text-only-v1` 결정론적 대체 릴리스도 구현했습니다. 이 프로필은 현재 스냅샷의 공고 텍스트 벡터 1,516개만 남기고 사진 1,228개·crop 1,002개 벡터, 원격 사진 URL, 사진 품질·VLM·detector 필드와 샘플 자산을 제외하며 런타임에서 같은 계약을 fail-closed로 검사합니다. 이는 새 배포물의 권리 노출을 줄이는 기술적 대안이지 기존 원격 Git 이력의 노출이나 권리 상태를 해결하는 법적 판단이 아닙니다. 전체 계약과 생성 절차는 [공개 텍스트 전용 배포 가이드](docs/public-text-only-release.md)에 기록합니다.

제공기관에 보낼 질문과 응답 기록 항목은 [공고 사진·파생 벡터 이용범위 확인 문안](docs/data-rights-inquiry-template.md)에 정리합니다.

## 현재 추적 스냅샷

현재 저장소의 정적 검색 스냅샷은 2026-07-26에 365일 범위의 공공 API를 다시 확인한 활성 공고 1,516건으로 구성됩니다. 생성 시각·조회 범위·모델·행 수·파일 해시는 [`data/snapshot_manifest.json`](data/snapshot_manifest.json)에 기록합니다.

| 파일 | 역할 | 현재 상태 |
| --- | --- | --- |
| `data/dog_metas.json` | 인덱스 행과 대응하는 검색 메타 | 3,746행, 활성 공고 1,516건; 공식 API의 `제한 없음` 표시와 출처를 따르며 코드의 Apache-2.0 적용 대상 아님 |
| `data/dog_faiss.index` | 전체 사진·Faster R-CNN crop·텍스트 CLIP 인덱스 | 3,746벡터, 512차원; 원천 데이터 파생물의 재배포 범위 확인 필요 |
| `data/snapshot_manifest.json` | 생성 조건·모델·커버리지·SHA-256 | 저장소에 포함 |
| `data/active_index_sync_report.json` | 유지·삭제·갱신·실패·무결성 검증 기록 | strict/integrity PASS |
| `data/eval_queries.sample.json` | 설명 보강 회귀용 샘플 질의 | 10개 질의 |

선택 DINO 파일럿은 위 기본 스냅샷을 교체하지 않고 Git 제외 산출물로 유지합니다. 최신 분리 shadow 스냅샷은 활성 공고 1,212건 중 detector crop이 있는 1,138건을 DINOv3 768차원 벡터로 저장하며 manifest가 원본 메타데이터, 인덱스와 행 메타의 SHA-256를 기록합니다. flow head는 CLIP 외형 텍스트를 이 DINO 공간에 정렬합니다. 별도 행동 head artifact도 연구 재현용으로 보존하지만 외부 평가 gate 실패 후 기본 비활성화했으며 `public-text-only-v1`에는 어떤 사진 파생 벡터나 DINO head도 포함하거나 로드하지 않습니다.

현재 `dog_metas.json`은 기준일(2026-07-26)에 active인 1,516건만 포함하며 공고 상태, 종료일, 지역, 보호소, 마지막 확인 시각이 모두 채워져 있습니다. 원본 사진 벡터 1,228개, 기존 객체 crop 1,002개, 공개 공고 텍스트 벡터 1,516개로 구성되며 사진 품질은 1,228/1,516건(81.0026%)에 있습니다. 사진 다운로드·갱신에 실패한 288건도 텍스트 임베딩 실패가 0건이므로 text-only 후보로 검색할 수 있습니다. 기준일 이후 상태 변경 가능성은 남아 있어 실제 문의와 제출·시연 직전에 다시 확인해야 합니다.

품종은 1,516건 모두 `public_notice_reported` 출처의 공고 표기값입니다. 그중 원문에 믹스가 직접 명시된 1,248건만 `mixed_breed=true`이며, 나머지 268건은 순종 여부를 확인할 근거가 없어 `null`입니다. `mixed_breed=false`로 확정한 공고는 없습니다.

이번 증분 벡터 생성은 로컬 CUDA 환경의 `openai-clip 1.0.1`로 실행했습니다. 제출 runtime은 공식 OpenAI CLIP 저장소의 commit `dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1`을 고정하지만, 현재 증분 벡터가 그 공식 commit으로 생성됐다는 뜻은 아닙니다. 두 실행 provenance는 manifest와 [active 동기화 리포트](data/active_index_sync_report.json)에 구분해 기록합니다.

## 생성·갱신 흐름

제출용 갱신에서는 365일 범위로 다시 받은 공공 API cache를 기준일의 authoritative 공고 집합으로 취급합니다. 운영 artifact를 제자리에서 수정하지 않고 다음 순서로 별도 출력에 생성합니다.

1. `scripts/fetch_live_dogs.py --days 365`로 공고를 수집하고 조회 범위, `fetched_at`, 원시·유지·종료 건수를 기록합니다.
2. `scripts/reuse_image_enrichment.py`는 공고 ID뿐 아니라 원본 사진 URL이 정확히 같을 때만 기존 `image_attrs`를 재사용합니다. URL이 없거나 다르거나 한 ID에 출처 URL이 충돌하면 재사용하지 않고 통계에 남깁니다.
3. `scripts/sync_active_index.py`는 fresh cache를 공고 메타의 기준으로 삼아 active ID만 동기화합니다. 기존 이미지·crop 벡터는 임베딩에 사용한 URL이 현재 원본 URL과 같을 때만 유지하고, 텍스트 벡터는 공개 공고 텍스트의 SHA-256이 같을 때만 유지합니다. 신규 이미지 다운로드는 정확한 공개 호스트 allowlist만 허용하고 redirect를 따르지 않으며, 기본 호스트는 `openapi.animal.go.kr`입니다.
4. 공고 텍스트가 바뀌면 예전 텍스트 행을 제거하고 다시 임베딩합니다. 원본 사진 URL이 바뀌면 예전 전체 이미지·crop 행을 모두 제거하고 새 원본 전체 이미지만 다시 임베딩하며, 증분 동기화에서는 crop을 새로 만들지 않습니다.
5. 새 사진의 다운로드·인코딩이 실패해도 공개 공고 텍스트 벡터가 성공하면 text-only 후보로 남깁니다. 공개 텍스트 임베딩 실패, active 공고의 전체 벡터 누락 또는 텍스트 벡터 누락은 출력 교체 없이 실패합니다.
6. 동기화는 VLM 필드와 생성 성격·활동성·아동 친화성·합사 가능성 같은 파생 행동 필드를 복사하거나 만들지 않습니다.
7. 인코딩 전에 최소 active 공고 수와 기존 ID 제거 비율을 검사하는 fail-closed gate를 통과해야 합니다. 이후 FAISS L2 형식, 차원, 벡터·메타 행 수, active 상태, 필수 텍스트 벡터, 금지 파생필드, content hash와 파일 SHA-256을 임시 출력에서 검증한 뒤에만 추적 artifact를 교체합니다.

동기화 report에는 유지·삭제·갱신·신규 벡터 수, 사진·텍스트 실패 수, 안전 gate 결과, 입력·출력 파일 해시와 canonical content hash를 기록하고 `data/snapshot_manifest.json`과 함께 보존합니다.

짧은 lookback은 공고일이 오래됐지만 아직 보호 중인 개체를 authoritative 집합에서 누락시켜 정상 벡터까지 삭제할 위험이 있습니다. 빠른 상태 확인에는 `--days N`을 사용할 수 있지만 정식 갱신에는 사용하지 않으며, 365일 범위도 그보다 오래 보호된 공고를 놓칠 수 있으므로 이전 manifest 대비 공고 수·제거 비율과 공공 API 페이지 완주 여부를 반드시 확인합니다.

Gemma/VLM 외형 설명과 Faster R-CNN crop 생성은 별도 연구 artifact에서만 수행합니다. 생성 시각, 원천 API, 레코드 수, 동기화 정책, 파일 해시와 사용한 모델 버전은 릴리스마다 manifest를 갱신합니다.

## 필드와 결측 처리

- 나이·체중·성별·중성화·지역·공고상 품종 표기·설명·공고 상태는 원문 메타를 우선합니다.
- 공고상 품종은 혈통 확인값이 아닙니다. 믹스가 직접 명시된 경우만 `mixed_breed=true`로 두고 특정 품종명만으로 순종 여부나 행동 특성을 추정하지 않습니다.
- 색상·발견일·전체 사진 URL·기관 갱신시각·건강검사·예방접종·사회성 원문 메모를 보존하되, 검사·접종 항목을 건강 결과로 재해석하지 않습니다.
- 연령은 명시적 연령 그룹을 우선하고, 숫자 나이 또는 출생연도로 계산할 때 `1세 이하=puppy`, `1세 초과 8세 미만=adult`, `8세 이상=senior`로 구간화합니다. 해석할 수 없으면 `unknown`입니다.
- 크기는 명시적 `size`·`size_hint`를 우선합니다. 없을 때 양수 체중을 `8kg 이하=small`(내부 `3kg 이하 tiny`도 화면에서는 small), `8kg 초과 18kg 이하=medium`, `18kg 초과=large`로 구간화하며, 체중도 없으면 검증된 외형 힌트를 마지막 보조값으로 사용합니다. 근거가 없으면 `unknown`입니다.
- 값이 없거나 `Unknown`, `미상`, 빈 문자열이면 확정값으로 보지 않습니다.
- 현재 제출 artifact와 평가에는 VLM 생성 속성·설명과 생성 행동·성격 필드가 없습니다. 선택 연구에서 VLM 외형 관찰값을 만들더라도 공고 원문과 분리하고 사진 관찰값으로만 표시합니다.
- 성격, 공격성, 활동성, 아동 친화성, 다른 동물과의 사회성은 사진·품종으로 추정하거나 `appearance` 후보 순위에 사용하지 않습니다. 기본 데모에서는 사용자의 관련 선호를 선택 후보에 관해 보호소에 확인할 질문으로만 변환합니다. 레거시 DINO 행동 경로는 외부 기관 분리 평가를 통과하지 못해 기본 비활성화했으며, 연구 재현에서 명시적으로 켜더라도 정보 없음과 상충 표현은 `unknown`이고 그 점수는 실제 성격 정답이 아닙니다.
- 종료·반환·입양 완료·자연사·안락사 등 비활성 공고는 운영 검색에서 제외합니다.

## 대표성·편향

- 온라인 공고에 올라온 개체만 포함하므로 전체 보호동물을 대표하지 않습니다.
- 지역·보호소별 공고 작성 방식과 사진 품질의 차이가 검색 노출 차이로 이어질 수 있습니다.
- 짧은 설명과 누락 필드가 많은 보호소의 개체는 근거 커버리지가 낮아질 수 있습니다.
- 사진 품질은 탐색 편의성일 뿐 건강·입양 적합성 점수가 아닙니다.
- 공고는 수시로 종료되므로 원격 링크와 상태는 마지막 확인 시각 이후 달라질 수 있습니다.

[고정 Top-10 노출 진단](docs/evaluation/exposure_representation.appearance_v1.md)은 region·보호소·크기·연령·사진 품질별 corpus 구성비, 노출 점유율과 명시적 `unknown`을 기록합니다. region은 공고 원문의 정식 시·도명을 포함해 1,516/1,516건(100%), 사진 품질은 1,228/1,516건(81.00%)에 근거가 있으며 나머지는 추정하지 않습니다. 이 진단은 해당 스냅샷과 12개 고정 질의만 설명하며 규범적 공정성 판단이 아닙니다.

[Held-out 두 번째 사진 평가](docs/evaluation/heldout_image_retrieval.appearance_v1.md)는 시각 인덱스가 있는 1,228개 활성 공고 중 120개를 지역·크기 층화 고정 표본으로 삼았습니다. 실행 시 primary payload는 85/120건(70.83%), secondary는 55/120건(45.83%) 다운로드됐고 primary와 raw SHA-256가 같은 26건을 중복으로 제외해 29/120건(24.17%)만 검색 지표의 분모가 됐습니다. 따라서 보고서의 조건부 Hit@K는 29건, end-to-end Hit@K는 120건을 분모로 하며 감사 커버리지는 `partial`입니다. URL, payload hash, 크기와 순위만 보고서에 남기고 내려받은 이미지와 디코딩 결과는 메모리에서 닫아 저장·재배포하지 않습니다. 공개 URL이 사라질 수 있고 받지 못한 primary와의 중복은 배제할 수 없습니다.

## 개인정보·보존

공개 공고의 보호소 연락처 외에 입양 신청자, 신고자 또는 개인 소유자의 개인정보를 수집·저장하지 않습니다. 원천 API가 삭제하거나 비공개 처리한 레코드는 운영 인덱스 갱신 시 제거하고, 평가용 보존본은 외부 배포 없이 식별·접근 정책을 별도로 적용해야 합니다.

## 샘플 자산

출처가 기록되지 않았던 AI 생성 이미지 `assets/samples/jindo.webp`와 `assets/samples/pome.jpg`는 2026-07-18에 제거했습니다. 현재 `assets/samples/`의 네 이미지는 2026-07-17 활성 공고 스냅샷에 포함된 실제 공고 사진을 TorchVision Faster R-CNN으로 개 영역만 잘라낸 검색 입력 예시입니다. 생성형 편집은 하지 않았습니다.

각 파일의 공고번호, 제공 지자체·보호소, 원본 사진 URL, 실제 공고 링크, 마지막 확인 시각, 변환 방식과 SHA-256은 `assets/samples/manifest.json`에 기록합니다. 샘플 선정은 검색 시연을 위한 외형·연령·크기 다양성 기준이며 입양 적합성 평가나 추천을 뜻하지 않습니다.

공공데이터포털에는 원천 API의 이용허락범위가 `제한 없음`으로 표시되어 있으나, API 응답에는 개별 사진의 촬영자·별도 라이선스가 없습니다. 따라서 샘플 사진과 그 crop은 프로젝트 소스의 Apache-2.0으로 재허가하지 않으며, 권리 요청이 접수되면 해당 파일을 제거하고 원격 공고 링크 방식으로 전환합니다. 공고 상태와 링크는 스냅샷 이후 변경될 수 있습니다.

샘플 네 개를 제거하는 것만으로 사진 관련 권리 검토가 끝나지는 않습니다. 추적 FAISS 인덱스에는 원본 사진 벡터 1,228개와 crop 벡터 1,002개가 있습니다. 따라서 공개 릴리스 여부는 이 사진 파생 산출물의 재배포 범위를 함께 확인한 뒤 결정합니다. 공고 텍스트·메타데이터와 텍스트 벡터는 공식 API의 `제한 없음` 표시와 출처를 별도 근거로 기록합니다.
