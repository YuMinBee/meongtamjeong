# 멍탐정 데이터 카드

## 개요

멍탐정은 국가동물보호정보시스템의 구조동물 공고를 검색 가능한 메타데이터와 임베딩으로 변환합니다. 데이터는 입양 가능성을 보증하거나 개체의 성격·건강·사회성을 판정하기 위한 것이 아니라, 보호소 방문 전 공고 탐색을 돕기 위해 사용합니다.

## 원천 데이터

- 제공기관: 농림축산식품부 농림축산검역본부
- 서비스: 국가동물보호정보시스템 구조동물 조회 서비스
- 공식 문서: https://www.data.go.kr/data/15098931/openapi.do
- API base: `https://apis.data.go.kr/1543061/abandonmentPublicService_v2`
- 공공데이터포털 표시 이용허락범위: 제한 없음
- API 키: 저장소에 포함하지 않으며 `ANIMAL_API_KEY` 환경변수로만 전달

공공 API의 이용허락범위와 별개로 사진 자체에 제3자 권리가 있을 수 있으므로, 원격 사진을 별도 데이터셋으로 재배포하기 전에는 권리와 출처를 다시 확인해야 합니다.

## 현재 추적 스냅샷

현재 저장소의 정적 검색 스냅샷은 2026-07-17에 공공 API에서 다시 확인한 활성 공고 1,394건으로 구성됩니다. 생성 시각·조회 범위·모델·행 수·파일 해시는 `data/snapshot_manifest.json`에 기록합니다.

| 파일 | 역할 | 현재 상태 |
| --- | --- | --- |
| `data/dog_metas.json` | 인덱스 행과 대응하는 검색 메타 | 4,100행, 활성 공고 1,394건 |
| `data/dog_faiss.index` | 전체 사진·Faster R-CNN crop·텍스트 CLIP 인덱스 | 4,100벡터, 512차원 |
| `data/snapshot_manifest.json` | 생성 조건·모델·커버리지·SHA-256 | 저장소에 포함 |
| `data/eval_queries.sample.json` | 설명 보강 회귀용 샘플 질의 | 10개 질의 |

현재 `dog_metas.json`은 공고 상태, 종료일, 지역, 보호소, 마지막 확인 시각이 모두 채워진 active-only 스냅샷입니다. 공고는 이후 종료될 수 있으므로 실제 방문 전 공고 링크와 보호소에서 상태를 다시 확인해야 합니다.

## 생성·갱신 흐름

1. `scripts/fetch_live_dogs.py`로 최신 공고를 수집합니다.
2. 선택적으로 `scripts/enrich_live_descriptions.py`를 실행하고, `scripts/enrich_image_crops.py`로 Faster R-CNN crop과 사진 품질을 생성합니다.
3. 기존 인덱스를 유지할 때는 `scripts/merge_dog_metadata.py`로 상태·지역·보호소 정보를 병합합니다.
4. active-only 검색 인덱스가 필요하면 `scripts/build_embeddings.py --exclude-unknown`으로 인덱스와 메타를 함께 재생성합니다.
5. `scripts/check_contest_readiness.py --strict`로 메타 커버리지와 인덱스 정합성을 확인합니다.

생성 시각, 원천 API, 레코드 수, 파일 해시와 사용한 모델 버전은 릴리스마다 manifest를 갱신합니다.

## 필드와 결측 처리

- 나이·체중·성별·중성화·지역·품종·설명·공고 상태는 원문 메타를 우선합니다.
- 값이 없거나 `Unknown`, `미상`, 빈 문자열이면 확정값으로 보지 않습니다.
- VLM 관찰값은 공고 원문과 분리하고 사진에서 관찰된 보조 속성으로 표시합니다.
- 성격, 공격성, 아동 친화성, 다른 동물과의 사회성은 명시적 근거가 없으면 추정하지 않습니다.
- 종료·반환·입양 완료·자연사·안락사 등 비활성 공고는 운영 검색에서 제외합니다.

## 대표성·편향

- 온라인 공고에 올라온 개체만 포함하므로 전체 보호동물을 대표하지 않습니다.
- 지역·보호소별 공고 작성 방식과 사진 품질의 차이가 검색 노출 차이로 이어질 수 있습니다.
- 짧은 설명과 누락 필드가 많은 보호소의 개체는 근거 커버리지가 낮아질 수 있습니다.
- 사진 품질은 탐색 편의성일 뿐 건강·입양 적합성 점수가 아닙니다.
- 공고는 수시로 종료되므로 원격 링크와 상태는 마지막 확인 시각 이후 달라질 수 있습니다.

## 개인정보·보존

공개 공고의 보호소 연락처 외에 입양 신청자, 신고자 또는 개인 소유자의 개인정보를 수집·저장하지 않습니다. 원천 API가 삭제하거나 비공개 처리한 레코드는 운영 인덱스 갱신 시 제거하고, 평가용 보존본은 외부 배포 없이 식별·접근 정책을 별도로 적용해야 합니다.

## 샘플 자산

`assets/samples/jindo.webp`와 `assets/samples/pome.jpg`의 원저작자·라이선스가 현재 저장소에 기록되어 있지 않습니다. 출품 릴리스 전 권리를 확인해 출처를 기록하거나, 팀이 직접 촬영한 이미지 또는 재배포가 허용된 이미지로 교체해야 합니다.
