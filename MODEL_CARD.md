# 멍탐정 모델·시스템 카드

## 시스템 요약

멍탐정은 하나의 생성 모델이 아니라 여러 검색·규칙 모듈을 조합한 멀티모달 탐색 시스템입니다.

1. CLIP이 질의 이미지와 텍스트를 임베딩합니다.
2. FAISS가 벡터 후보를 검색합니다.
3. BM25, 공고 사실 기반 구조화 조건과 경량 그래프 근거가 외형 후보를 보강합니다.
4. 권장 `ranking_scope=appearance`가 검색 점수를 유지하면서 공고상 크기·연령·지역과 사진 품질로 방문 전 확인 순서를 보조합니다.
5. 후보를 고른 뒤 주거·부재·활동·경험·아동·다른 동물·선호 성격을 보호소 확인 질문으로 변환합니다.
6. 하위 호환 `profile` scope와 Gemma·VLM 경로는 기본 제출 데모와 분리된 실험 기능입니다.
7. 선택형 DINO 롤아웃은 frozen DINOv3 이미지와 flow 정렬 CLIP 텍스트를 같은 768차원 공간에서 검색하며, 기본값은 `off`입니다.

## 의도된 용도

- 자연어 또는 참고 이미지로 실제 유기견 공고 후보 탐색
- 공고에서 확인 가능한 크기·연령·희망 지역과 사진 품질에 따른 방문 전 후보 정렬 보조
- 사용자의 생활·성격 선호를 선택 후보에 관해 보호소에 물을 질문으로 변환
- 보호소·동물보호단체가 자체 데이터에 적용할 수 있는 검색 파이프라인 예시

## 의도하지 않은 용도

- 입양 승인, 적합성 확정 또는 자동 입양 결정
- 공격성·건강·성격·아동 친화성·합사 가능성의 사진 기반 추정
- 품종, 질병, 행동 문제의 진단
- 종료 여부를 확인하지 않은 공고에 대한 입양 가능 단정
- 사용자 프로필을 이용한 차별적 자격 심사
- 문의 문구의 자동 전송, 공식 입양 신청 또는 상담 예약 처리

## 입력과 출력

- 권장 검색 입력: 자연어 질의 또는 참고 이미지, 외형 조건, 크기·연령·희망 지역. 9개 프로필 필드는 API 호환상 유지되지만 생활 필드는 `appearance` 순위에 쓰이지 않습니다.
- 검색 출력: 후보, 검색/외형 조건/사진 품질/최종 점수, 확인된 조건, 정보 없음, 실제 공고 링크
- 문의 입력·출력: 서버가 대조할 공고 ID와 선택적 생활·성격 선호를 받아 확인 질문, 전화 스크립트, 복사 가능한 이메일·문의폼 문구를 반환합니다.

점수는 후보 정렬을 위한 내부 상대값이며 입양 적합도나 성격 확률이 아닙니다. 사용자 화면에서는 점수만 강조하지 않고 공고 근거 수와 확인 필요 항목을 함께 제공해야 합니다. 문의 문구는 자동 전송되지 않으며 공식 입양 신청이나 입양 확정을 뜻하지 않습니다.

## 모델과 실행 모드

| 구성요소 | 기본 역할 | 필수 여부 |
| --- | --- | --- |
| OpenAI CLIP ViT-B/32 | 이미지·텍스트 임베딩 | 핵심 검색에 필요 |
| Meta DINOv3 ViT-B/16 | 선택형 crop 이미지 임베딩 | 연구 롤아웃에서만 필요 |
| CLIP→DINO flow head | frozen CLIP 텍스트를 DINO 공간에 정렬 | 연구 롤아웃에서만 필요 |
| 공고문 행동 head(512→128→3) | 명시된 세 행동 축의 검색 보조 | 레거시 이미지+프로필 연구 경로에서만 선택 |
| FAISS | 근접 벡터 검색 | 핵심 검색에 필요 |
| BM25/규칙/그래프 | 텍스트·공고 사실 근거 보강 | 코드에 포함 |
| 규칙 기반 문의 생성기 | 생활·성격 선호를 보호소 확인 질문으로 변환 | 코드에 포함 |
| Gemma 3/VLM | 문장·외형 설명 연구 경로 | 선택·제출 평가 제외 |
| TorchVision Faster R-CNN MobileNetV3-FPN | 오프라인 객체 영역 추출 | 인덱스 보강 시 선택 |

Gemma, DINOv3, CLIP→DINO flow와 행동 head 가중치는 저장소에 포함하지 않습니다. Faster R-CNN은 TorchVision의 COCO 사전학습 가중치를 첫 실행 시 내려받으며, 각 모델의 출처·학습 데이터·이용 조건을 확인한 사용자가 준비해야 합니다. 현재 제출 artifact와 평가에는 Gemma/VLM이 생성한 행동·성격 속성이 없습니다. 탐지기 선택 근거와 제한은 [탐지기 비교 기록](docs/detector-comparison.md)에 정리합니다.

### 배포 프로필 경계

기본 `full` 프로필은 사진·crop·공고 텍스트 벡터와 사진 품질 신호를 포함하며 아래의 고정 평가도 이 산출물만 설명합니다. 선택형 `public-text-only-v1`은 권리 노출 최소화를 위해 검증된 공고 텍스트 벡터만 남기고 시각 벡터·원격 사진 URL·사진 품질 신호를 제거합니다. 이 대안에서도 사용자 업로드 이미지는 CLIP 호환상 받을 수 있지만 검색 대상이 공고 텍스트 벡터뿐이므로 `full`의 시각 검색 성능을 귀속하지 않습니다. 프로필 marker와 실제 index·metadata 계약이 다르면 런타임은 시작하지 않습니다.

따라서 아래 `full` 평가 수치와 held-out 교차사진 결과는 `public-text-only-v1`의 성능 근거가 아닙니다. text-only를 최종 출품 프로필로 고르면 적용 가능한 평가를 해당 산출물에서 다시 만들고 시각 평가는 `N/A`로 표시해야 합니다. 자세한 생성·패키징·한계는 [공개 텍스트 전용 배포 가이드](docs/public-text-only-release.md)에 있습니다.

### DINO 연구 롤아웃 경계

`DINO_FUSION_MODE`는 `off`, `shadow`, `active`를 지원합니다. `off`는 DINO 의존성과 artifact를 로드하지 않습니다. `shadow`는 기존 CLIP 결과를 제공하면서 DINO 후보만 관찰하고, `active`만 DINO 후보를 실제 하이브리드 벡터 점수로 사용합니다. 모든 artifact는 SHA-256와 원본 `dog_metas.json` 해시가 맞아야 하며 기본 `DINO_FUSION_FAIL_OPEN=true`에서는 오류 시 CLIP으로 복귀합니다. 외부 평가에 실패한 공고문 약라벨 행동 head는 `DINO_FUSION_BEHAVIOR_ENABLED=false`가 기본이고 연구 재현에서만 명시적으로 켭니다. `public-text-only-v1`에서는 설정과 무관하게 DINO 시각 인덱스를 사용하지 않습니다.

행동 head는 사진으로 성격을 추론하지 않습니다. 공고 원문의 명시적 양·음 표현으로 만든 결측 인식 silver label을 학습하며, 문구가 없거나 충돌하면 unknown으로 둡니다. 권장 `/search/appearance/image`에는 적용되지 않고 `/recommend_with_image`의 명시적 성격 문구에만 최대 25% 보조 신호로 적용됩니다. 이 확률과 점수는 실제 성격 또는 입양 적합도 확률이 아닙니다.

2026-07-26 증분 인덱스 갱신은 로컬 CUDA 환경의 `openai-clip 1.0.1`로 실행했습니다. 제출 runtime은 공식 OpenAI CLIP commit `dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1`을 고정하지만, 이 차이를 숨기거나 현재 증분 벡터가 공식 commit으로 생성됐다고 주장하지 않습니다. 상세 provenance와 artifact hash는 [`data/snapshot_manifest.json`](data/snapshot_manifest.json) 및 [동기화 리포트](data/active_index_sync_report.json)에 있습니다.

## 현재 평가

2026-07-26 기준 고정 질의 12개와 active-only 인덱스에서 상태 필터가 같은 `clip_active`와 자연어 `hybrid_natural_graph`를 비교했습니다. 공고 원문의 색상·체중·연령·지역·상태만으로 검색 전에 silver qrels를 만들었으며 결과 설명과 VLM 출력은 라벨에 사용하지 않았습니다. 따라서 아래 수치는 공고 사실 기반 자연어 조건 검색의 회귀 지표이며 주관적 외형 유사도 성능이 아닙니다. 명시적 구조 조건을 넣은 oracle은 대표 성능이 아닌 상한선으로 분리했습니다.

| 지표 | `clip_active` | `hybrid_natural_graph` |
| --- | ---: | ---: |
| Precision@5 | 11.67% | 85.00% |
| Recall@10 | 0.77% | 41.89% |
| nDCG@5 | 11.65% | 89.53% |
| MRR | 0.211 | 0.917 |
| Hit@5 | 33.33% | 100.00% |
| inactive exposure@10 | 0.00% | 0.00% |
| 평균 지연 | 20.6ms | 152.1ms |

입력 파일 해시, 전체 결과와 재현 조건은 [검색 평가 리포트](docs/evaluation/retrieval_eval.appearance_v1.md)에 기록합니다. 기본 게이트는 자연어 대표 시스템의 `Precision@5 ≥ 70%`, `Hit@5 ≥ 90%`, inactive exposure `= 0%`입니다. 현재 코퍼스 자체가 active-only라 노출 0%는 스냅샷 무결성 결과이고, 종료 공고 필터 동작은 active·inactive 혼합 회귀 테스트로 별도 검증합니다. 이 수치는 독립 미공개 테스트나 사람의 시각적 관련성, 입양 성과, 성격 또는 생활 적합도를 의미하지 않습니다.

[프로필 재정렬 리포트](docs/evaluation/profile_rerank.appearance_v1.md)는 같은 12개 후보를 두 외형 프로필로 재정렬해 top-1 변화, unknown 중립성, 점수·근거 일관성을 확인한 결정적 구현 계약입니다. 품질 가중치 `0.00`→`0.05` 민감도에서는 두 프로필 모두 top-1과 Top-5 구성은 유지됐지만, 품질 unknown은 직접 감점이 없어도 known 후보의 가산으로 상대적으로 불리해질 수 있음을 별도로 기록했습니다. 해당 P@5·nDCG·MRR 변화와 품질 민감도는 독립 성능·최적 가중치 근거가 아닙니다. [비추론 안전 계약](docs/evaluation/safety_contract.appearance_v1.md)은 외형 질의 동치, 생활정보 순위 불변, 활동량을 포함한 문의 질문 변화, 금지 표현 차단을 4/4 통과했습니다. blind human relevance와 프로필별 후보 선택 시간은 아직 측정하지 않았습니다.

[노출·대표성 및 증거 커버리지 진단](docs/evaluation/exposure_representation.appearance_v1.md)은 랭킹을 다시 실행하지 않고 동일 Top-10을 후처리합니다. 120개 슬롯의 고유 공고는 `clip_active` 50건, `hybrid_natural_graph` 116건이며 공개 필드 증거 커버리지는 99%와 100%였습니다. region은 공고 원문 기준 1,516/1,516건 정규화했고 사진 품질 288건은 corpus에서 `unknown`으로 보존합니다. 이 결과는 고정 질의의 기술 진단이지 보호특성이나 규범적 공정성을 평가한 것이 아닙니다.

[Held-out 두 번째 사진 평가](docs/evaluation/heldout_image_retrieval.appearance_v1.md)는 120개 고정 공고를 시도해 primary 85건(70.83%), secondary 55건(45.83%)을 내려받았고 동일 payload 26건을 제외해 29건(24.17%)을 평가했습니다. 평가 가능 29건 기준 Hit@1은 82.76%, Hit@5·10은 96.55%, MRR은 0.8912, 중앙 순위는 1위였습니다. 최초 120건을 분모로 한 end-to-end Hit@1·5·10은 20.00%·23.33%·23.33%이고 감사 상태는 `partial`입니다. 질의 encoder의 공식 CLIP commit과 선언된 artifact 결합은 확인했지만 기존 벡터 2,045개에 per-vector fingerprint가 없어 전체 인덱스 encoder는 검증하지 못했습니다. 이 과제는 같은 공고의 시각적 동일성 검색 대리 지표이며 서로 다른 개의 주관적 유사도나 입양 적합성을 뜻하지 않습니다.

별도 1,002-crop DINO 파일럿의 평가 가능 표본은 20~21개로 작습니다. 같은 코퍼스의 crop 기준 동일 개체 검색은 CLIP Hit@1 80.95%, DINOv3 90.48%였고, 80% 이미지+20% 정렬 텍스트 검색은 CLIP 기준 Hit@1 85%·MRR 0.8932에서 DINO+flow Hit@1 95%·MRR 0.9600으로 개선됐습니다. 한국어 속성 nDCG@10은 0.4058에서 0.5375로 변했습니다. 외부 PetFinder 기관 분리 test 77마리에서는 CLIP 이미지 Recall@10 0.557, DINOv3 0.877이었지만 성격 융합 nDCG는 DINO 단독 0.533보다 낮은 0.516이었고 학습한 호환성 head의 multimodal macro AUC도 0.548에 그쳤습니다. 추가 v2/v3·멀티뷰·텍스트 비중 ablation 역시 적용 gate를 통과한 변경이 없었습니다. 따라서 시각 개선 신호와 성격 실패 결과를 분리하며, 이는 사람 관련성 평가나 실제 성격 정답·운영 성과 주장이 아닙니다.

## 한계와 위험

- 정적 인덱스는 최신 공고 상태를 자동 보증하지 않습니다.
- 공고 설명의 결측과 표현 차이가 검색·근거 커버리지에 영향을 줍니다.
- 현재 제출 artifact에는 VLM 파생 속성이 없습니다. 선택 연구에서 만든 외형 관찰값도 공고 사실이나 행동 평가가 아닙니다.
- DINO 파일럿은 20~21개 질의와 현재 로컬 crop에 제한되며, DINOv3가 모든 질의나 DINOv2보다 항상 우수하다는 근거가 아닙니다.
- 행동 head 평가는 학습 원천과 같은 공고문에서 만든 silver label 일관성 평가라 실제 성격 일반화를 입증하지 않습니다.
- 사진 품질 가산은 촬영 여건이 좋은 보호소에 노출 우위를 줄 수 있습니다.
- 외형 조건 점수는 크기·연령·지역 근거 커버리지를 반영하지만, 평가된 조건 수가 적은 결과는 확인 필요 항목을 함께 해석해야 합니다.
- 외부 모델·사진 URL·공공 API의 가용성과 약관 변경에 영향을 받습니다.

## 안전 원칙

- unknown을 사실처럼 채우지 않습니다.
- 권장 `appearance` 검색에서 주거·부재·활동·경험·아동·합사·성격을 순위 또는 개체 추정에 사용하지 않습니다.
- 이 정보는 선택 후보에 관해 보호소가 실제로 관찰한 내용을 묻는 질문에만 사용합니다.
- 비활성 공고를 필터링하고 상태 미확인 공고는 별도로 표시합니다.
- `/adoption/contact_card`는 서버측 공고 ID와 상태를 대조하며, 신뢰된 active 레코드에만 직접 전화·메일 링크를 제공합니다.
- 전화·이메일/문의폼 초안은 복사만 제공하고 자동 전송·공식 신청·적합성 확정을 하지 않습니다.
- 실제 성격·건강·생활 적합성은 보호소 방문과 상담으로 확인하도록 안내합니다.
