# PetFinder 영어 속성 정렬 학습 v1

2026-09-17. 이 문서는 실험 설정이며 성능 개선 주장이 아니다.

- 원본: PetFinder.my Adoption Prediction 대회 ZIP. D:/meongtamjeong_research/petfinder에 보관한다.
- 대상: Type=1, Quantity=1, 사진·설명문·색상·크기가 있는 공고. 설명문은 존재 여부만 확인하며 이번 학습 입력에는 쓰지 않는다.
- 원래 test 집합을 유지한다. 해당 테스트 사진의 특징 추출과 성능 평가는 학습·모델 선택 이후로 미룬다.
- 대상 공고의 모든 사진에 바이트 SHA256을 계산하고, 대표 사진에는 크기+디코딩 RGB 해시도 계산한다. 서로 다른 PetID가 같은 해시를 공유하면 관련 공고 모두 제외한다. 유사 사진과 재등록 개체의 완전한 중복 제거는 아직 보장하지 않는다.
- 남은 원래 train 공고에서 RescuerID를 묶어 고정 해시 순서로 등록자 약 15%를 검증에 배정한다. 원래 test와 train의 등록자 중복은 별도로 보고한다. 테스트는 미등록자 일반화 평가로 부르지 않는다.
- 공고당 번호가 가장 작은 대표 사진 한 장을 사용한다. 모든 사진 학습 및 다른 사진으로 같은 개체 찾기 평가는 이번 실행에 포함하지 않는다.
- CLIP ViT-B/32와 DINOv3 ViT-B/16은 고정한다. DINO 리비전은 5931719e67bbdb9737e363e781fb0c67687896bc이다. 이미지·텍스트 특징을 한 번 계산하여 재사용한다.
- 주 조건은 영어 색상(최대 세 가지)·크기 템플릿이다. 학습 표현 세 개와 검증 표현 한 개를 구분한다. 자연어 원문 번역이나 성격 추론은 하지 않는다.
- 목적함수: 같은 색상 조합·크기의 공고를 양성으로 하는 다중 양성 대조 손실과 학습 집합 시각 중심점 정렬. 검증에는 중심점 항을 쓰지 않아 학습에 없는 속성 조합도 검증할 수 있다.
- CLIP 텍스트→DINO의 Linear/MLP/Flow와 CLIP 텍스트→CLIP 이미지의 Linear 대조군을 학습한다. 후자는 도메인 학습 효과와 DINO 추가 효과를 구분하기 위한 대조군이다.
- 고정 분할, 시드 17/29/43. AdamW, 학습률 0.002, weight decay 0.0001, 배치 512, 최대 150 epoch. 5 epoch마다 검증, 6회 연속 개선 없으면 종료. 검증 손실 최소 체크포인트를 저장한다.
- 출력: D:/meongtamjeong_research/petfinder/english_v1. 기존 한국 공고 체크포인트를 덮어쓰지 않는다.

후속 평가는 영어를 주 언어로 고정하여 PetFinder test 및 한국·대만 공고에서 같은 후보·정답·입력으로 비교한다. 한국어 조건은 보조 평가다. 결합 비율은 PetFinder 검증에서 선택하고 외부 평가에 고정 적용한다. 한국·대만은 기존에 결과를 확인한 자료이므로 완전히 새로운 확증 평가로 취급하지 않는다. 색상·크기 검색 지표를 사용자 취향이나 얼굴 형태 보존의 직접 증거로 해석하지 않는다.
