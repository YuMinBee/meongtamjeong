# 보안 정책

## 지원 범위

기본 브랜치 `main`의 최신 코드와 최신 릴리스 태그를 지원 대상으로 봅니다. 과거 데이터 스냅샷이나 개인 포크는 별도로 지원하지 않습니다.

대회 릴리스 게이트의 Git 이력 검사는 원격에 게시될 수 있는 일반 ref의 finding을 실패로 처리합니다. 로컬 작업 복구용 `refs/codex/*`도 별도로 스캔·보고하지만 원격 제출 이력은 아니므로 tool-only finding 자체는 릴리스를 차단하지 않습니다. 같은 blob이 일반 ref에서도 도달 가능한 `mixed` 상태이면 차단합니다.

## 취약점 신고

API 키 노출, 인증 우회, 임의 파일 접근, 원격 코드 실행, 개인정보 노출처럼 민감한 문제는 공개 issue에 세부 재현 정보를 올리지 마세요. GitHub 저장소의 **Security > Report a vulnerability** 비공개 신고 기능을 우선 사용하세요. 해당 기능을 사용할 수 없을 때는 민감 정보를 제외한 최소 설명으로 관리자에게 비공개 연락 방법을 요청하세요.

신고에는 영향받는 버전·커밋, 재현 조건, 예상 영향과 가능한 완화책을 포함해 주세요.

## 비밀정보 관리

- `.env`는 커밋하지 않습니다.
- `API_KEY`, `ANIMAL_API_KEY`, Hugging Face/GitHub 토큰은 환경변수 또는 비밀 저장소로 전달합니다.
- 로그·오류 응답·시연영상에 키나 전체 요청 헤더를 노출하지 않습니다.
- 유출이 의심되면 즉시 키를 폐기·재발급하고 Git 기록에 값이 남았는지 확인합니다.

## 데이터 안전

공개 보호소 정보 외의 개인 식별정보를 수집하지 않습니다. 공고 원문에 예상치 못한 개인정보가 포함된 경우 인덱싱 전에 제거하고, 이미 포함된 artifact는 교체한 뒤 관련 캐시를 폐기합니다.

로컬 JSON도 신뢰 경계 밖의 입력으로 취급합니다. 원격 공고 사진을 처리하는 공개 CLI는 정확한 공개 호스트 allowlist만 요청하고 redirect를 따르지 않으며, 응답 크기·이미지 포맷·화소 수·decompression bomb 제한을 적용합니다. 새 provider를 연결할 때는 확인한 이미지 호스트만 반복 가능한 `--allowed-image-host` 옵션으로 명시하며, 안전 제한을 우회하기 위해 임의 호스트를 넓게 허용하지 않습니다.
